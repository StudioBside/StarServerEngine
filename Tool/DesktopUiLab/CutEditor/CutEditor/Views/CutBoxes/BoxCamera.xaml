<UserControl
    x:Class="CutEditor.Views.CutBoxes.BoxCamera"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:local="clr-namespace:CutEditor.Views.CutBoxes"
    mc:Ignorable="d" 
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:lcvt="clr-namespace:CutEditor.Converters"
    xmlns:converters="clr-namespace:Du.Presentation.Converters;assembly=Du.Presentation"
    xmlns:nkm="clr-namespace:NKM;assembly=Shared.Templet"
    xmlns:extensions="clr-namespace:Du.Presentation.Extensions;assembly=Du.Presentation"
    xmlns:models="clr-namespace:CutEditor.Model;assembly=CutEditor.Model"
    xmlns:viewmodels="clr-namespace:CutEditor.ViewModel;assembly=CutEditor.ViewModel"
    d:DataContext="{d:DesignInstance Type=viewmodels:VmCut}"
    d:DesignHeight="450" d:DesignWidth="800">

    <!--컨트롤 : 카메라-->
    <Border Background="{StaticResource CameraBoxBrush}"
                            Visibility="{Binding ShowCameraSection, Converter={converters:BoolToVisibilityConverter}}">
        <StackPanel Orientation="Horizontal" Margin="5">
            <ui:Button Margin="0,0,10,0">
                <TextBlock TextAlignment="Center">
                    <Run>CameraOffset</Run>
                    <LineBreak/>
                    <Run>종류</Run>
                </TextBlock>
            </ui:Button>

            <ui:Button Margin="0,0,10,0"
                       Content="CameraOffset 위치"/>

            <!--CameraOffsetTime-->
            <ui:DropDownButton Margin="0,0,10,0">
                <!--값이 없을 때는 한글 표기. 값이 있을때는 배경색을 다르게 표기-->
                <ui:DropDownButton.Style>
                    <Style TargetType="ui:DropDownButton" BasedOn="{StaticResource {x:Type ui:DropDownButton}}">
                        <Setter Property="Content" Value="{Binding Cut.CameraOffsetTime
                            , Converter={converters:EnumToParamConverter}, ConverterParameter=없음.Slow.Normal.Fast}"/>
                        <Setter Property="Background" Value="{StaticResource HasValueBrush}"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Cut.CameraOffsetTime}" Value="NONE">
                                <Setter Property="Content" Value="오프셋 타임"/>
                                <Setter Property="Background" Value="White"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ui:DropDownButton.Style>
                <ui:DropDownButton.Flyout>
                    <ContextMenu ItemsSource="{Binding Source={extensions:EnumValues {x:Type nkm:CameraOffsetTime}}}">
                        <ContextMenu.ItemTemplate>
                            <DataTemplate>
                                <MenuItem Header="{Binding .
                                    , Converter={converters:EnumToParamConverter}, ConverterParameter=없음.Slow.Normal.Fast}"
                       Command="{Binding DataContext.SetCameraOffsetTimeCommand, RelativeSource={RelativeSource AncestorType=ListViewItem}}"
                       CommandParameter="{Binding .}"/>
                            </DataTemplate>
                        </ContextMenu.ItemTemplate>
                    </ContextMenu>
                </ui:DropDownButton.Flyout>
            </ui:DropDownButton>
            
            <!--CameraEase-->
            <ui:Flyout Placement="Top" VerticalAlignment="Top" IsOpen="{Binding CameraEaseFlyoutOpen, Mode=TwoWay}">
                <ui:ListView Width="600" Height="400"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                             ItemsSource="{Binding Source={extensions:EnumValues {x:Type models:Enums+Ease}}}"
                             SelectedItem="{Binding Cut.CameraEase}">
                    <ui:ListView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel/>
                        </ItemsPanelTemplate>
                    </ui:ListView.ItemsPanel>
                    <ui:ListView.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Vertical" Margin="5">
                                <Border Background="Black" Width="160" Height="130"/>
                                <TextBlock Text="{Binding .}" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </DataTemplate>
                    </ui:ListView.ItemTemplate>
                </ui:ListView>
            </ui:Flyout>
            <ui:Button Margin="0,0,10,0" Content="CameraEase"
                       Command="{Binding PickCameraEaseCommand}"
                       ToolTip="{Binding Cut.CameraEase}">
                <ui:Button.Style>
                    <Style TargetType="ui:Button" BasedOn="{StaticResource {x:Type ui:Button}}">
                        <Setter Property="Background" Value="{StaticResource HasValueBrush}"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Cut.CameraEase}" Value="Unset">
                                <Setter Property="Background" Value="White"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ui:Button.Style>
            </ui:Button>

        </StackPanel>
    </Border>

</UserControl>
